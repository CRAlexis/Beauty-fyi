<ScrollView height="100%" opacity="0" visibility="collapsed" id="bookAppointmentSecondSlideContainer" xmlns:Card="@nstudio/nativescript-cardview">
    <StackLayout paddingTop="20" paddingBottom="20">
        <Card:CardView width="90%" margin="10" class="cardStyle" elevation="10">
            <StackLayout paddingBottom="20" xmlns:calendar="nativescript-ui-calendar">
                <calendar:RadCalendar
                    id="bookAppointmentCalander" 
                    height="250"
                    eventSource="{{ events }}" 
                    eventsViewMode="Inline" 
                    selectionMode="Single"
                    viewMode="Month"
                    dateSelected="{{ onDateSelected }}" 
                    dateDeselected="{{ onDateDeselected }}"
                    navigatedToDate="{{ onNavigatedToDate }}"
                    navigatingToDateStarted="{{ onNavigatingToDateStarted }}"
                    viewModeChanged="{{ onViewModeChanged }}"
                    inlineEventSelected="{{ onInlineEventSelected }}">
                    </calendar:RadCalendar>
                    <StackLayout padding="10" xmlns:lv="nativescript-ui-listview">
                        <Label text="Availability on the 24th of November" class="h4" textAlignment="center" textWrap="true" />
                        <StackLayout>
                            <lv:RadListView id="bookAppointmentAvaliableTimes" class="list-group" style="background-color: transparent"> 
                            <lv:RadListView.listViewLayout>
                                <lv:ListViewGridLayout  spanCount="3" />
                            </lv:RadListView.listViewLayout>
                            <lv:RadListView.itemTemplate >    
                                <StackLayout class="btn-light-grey-outline btn-rounded-sm" margin="5">
                                    <Label text="{{time}}" class="h4 font-bold" textAlignment="center" textWrap="true" />
                                </StackLayout>
                            </lv:RadListView.itemTemplate>
                        </lv:RadListView>
                        </StackLayout>
                    </StackLayout>
            </StackLayout>
        </Card:CardView>
    </StackLayout>

</ScrollView>